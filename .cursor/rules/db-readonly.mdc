---
description: 数据库铁律——只读查询，禁止任何写操作
alwaysApply: true
---

# 数据库操作铁律（死线）

对**任何**数据库（含测试库、生产库、本地库）：

1. **永远只能查询（SELECT / SHOW / DESCRIBE / 只读 EXPLAIN 等）**，用于核对与分析。
2. **严禁**由 Agent 执行任何会改变库的操作，包括但不限于：
   - INSERT / UPDATE / DELETE / REPLACE
   - CREATE / ALTER / DROP / TRUNCATE / RENAME
   - 存储过程/触发器中的写操作
   - 通过应用接口、脚本、mysql 客户端等间接改库
3. 凡需改结构或改数据：**只编写 SQL 文件交给用户**，由用户自行执行。不要替用户连库执行迁移/清理脚本。
4. 即使用户口头说「删表」「改数据」，也只交付 SQL，**不代执行**。

违反本条视为严重错误。
