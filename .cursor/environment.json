{
  "install": "mvn compile -DskipTests && cd ruoyi-ui && npm install",
  "terminals": [
    {
      "name": "Run Vue dev",
      "command": "cd ruoyi-ui && npm run dev"
    },
    {
      "name": "Run Spring Boot",
      "command": "mvn spring-boot:run -pl ruoyi-admin"
    }
  ]
}
