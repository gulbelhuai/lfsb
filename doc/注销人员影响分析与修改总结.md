# 注销人员影响分析与修改总结

## 📋 背景说明

在实现"人员注销登记"功能后,需要确保系统在各业务办理场景中默认排除注销人员(is_alive='0'),同时在综合查询/追溯场景中保留查询注销人员的能力。

## 🎯 核心原则

**有效人员定义**: 未删除(del_flag='0') 且 未注销(is_alive='1')

**应用场景**:
- ✅ **业务办理场景**: 默认只查健在人员(失地居民登记、村干部登记、补贴发放等)
- ✅ **综合查询/追溯**: 支持查询所有人(包括注销人员),用于历史数据追溯

---

## 📝 修改清单

### 1️⃣ 后端Service层

#### ✨ SubsidyPersonService & SubsidyPersonServiceImpl

**新增方法**:
```java
/**
 * 根据ID查询"有效人员"（排除注销人员）
 */
SubsidyPerson selectAliveSubsidyPersonById(Long id)
```

**已有方法说明**:
- `selectSubsidyPersonByIdCardNo(String idCardNo)` - 查询所有人(包括注销)
- `selectAliveSubsidyPersonByIdCardNo(String idCardNo)` - 只查健在人员 ✅
- `selectSubsidyPersonList(SubsidyPersonListReq req)` - 支持is_alive过滤条件

---

#### ✨ SubsidyDistributionServiceImpl (补贴发放服务)

**修改方法**:

1. **searchResidents()** - 自动搜索居民(下拉列表)
   ```java
   // 新增过滤条件
   wrapper.eq(SubsidyPerson::getIsAlive, "1"); // 排除注销人员
   ```

2. **getAvailableSubsidiesByPersonId()** - 根据居民ID查询可发放补贴
   ```java
   // 修改为使用新方法
   SubsidyPerson person = subsidyPersonService.selectAliveSubsidyPersonById(subsidyPersonId);
   if (person == null) {
       throw new ServiceException("未找到该居民信息或该居民已注销");
   }
   ```

3. **getAvailableSubsidies()** - 根据身份证号或姓名查询可发放补贴
   ```java
   // 新增过滤条件
   wrapper.eq(SubsidyPerson::getIsAlive, "1"); // 排除注销人员
   ```

---

#### ✨ ResidentQueryServiceImpl (居民综合查询服务)

**修改方法**:

1. **searchResidents()** - 搜索居民
   ```java
   // 新增过滤条件：默认排除注销人员
   wrapper.eq(SubsidyPerson::getIsAlive, "1");
   ```

2. **getResidentDetailInfo()** - 获取居民详细信息
   ```java
   // 保持现状：支持查询所有人(包括注销人员),用于历史追溯
   // 注释说明：不过滤is_alive,支持查询注销人员用于历史追溯
   ```

---

### 2️⃣ 后端Controller层

#### ✨ SubsidyPersonController

**修改接口**:

```java
/**
 * 根据身份证号查询被补贴人基础信息
 * 
 * @param idCardNo 身份证号
 * @param includeCancel 是否包含注销人员(默认false,只查健在人员)
 */
@GetMapping("/getByIdCardNo")
public AjaxResult getByIdCardNo(
        @RequestParam String idCardNo,
        @RequestParam(required = false, defaultValue = "false") Boolean includeCancel)
{
    SubsidyPerson subsidyPerson;
    if (includeCancel) {
        // 查询所有人(包括注销人员),用于综合查询/追溯
        subsidyPerson = subsidyPersonService.selectSubsidyPersonByIdCardNo(idCardNo);
    } else {
        // 默认只查健在人员,用于业务办理场景
        subsidyPerson = subsidyPersonService.selectAliveSubsidyPersonByIdCardNo(idCardNo);
    }
    return AjaxResult.success(subsidyPerson);
}
```

---

### 3️⃣ 前端API层

#### ✨ subsidyPerson.js

**修改API函数**:

```javascript
// 根据身份证号查询被补贴人基础信息
export function getSubsidyPersonByIdCardNo(idCardNo, includeCancel = false) {
  return request({
    url: '/shebao/subsidyPerson/getByIdCardNo',
    method: 'get',
    params: {
      idCardNo,
      includeCancel
    }
  })
}
```

---

### 4️⃣ 前端页面层

#### ✨ 注销登记页面调用更新

**修改文件**:
- `ruoyi-ui/src/views/shebao/person/cancel/index.vue`
- `ruoyi-ui/src/views/shebao/subsidyPerson/index.vue`

**修改内容**:
```javascript
// 注销登记需要查询所有人(包括已注销人员)以便验证和提示
getSubsidyPersonByIdCardNo(this.form.idCardNo, true).then(res => {
  // ...处理逻辑
})
```

---

## ✅ 已正确实现的业务模块

以下模块已经正确使用了`selectAliveSubsidyPersonByIdCardNo`方法,默认排除注销人员:

1. ✅ **TeacherSubsidyServiceImpl** - 教师补贴登记
2. ✅ **LandLossResidentServiceImpl** - 失地居民登记
3. ✅ **VillageOfficialServiceImpl** - 村干部登记

---

## 🔍 验证场景

### 场景1: 补贴发放 - 应排除注销人员
- [x] 搜索居民下拉列表
- [x] 根据身份证号查询可发放补贴
- [x] 根据居民ID查询可发放补贴

### 场景2: 业务办理 - 应排除注销人员
- [x] 失地居民登记时选人
- [x] 村干部登记时选人
- [x] 教师补贴登记时选人

### 场景3: 综合查询 - 应支持查所有人
- [x] 居民详情查询(支持查注销人员)
- [x] 人员列表查询(通过is_alive筛选)
- [x] 注销登记时验证人员(需要查所有人)

---

## 📊 数据库字段说明

**shebao_subsidy_person表**:
- `is_alive`: 是否健在 ('0'-否/已注销, '1'-是/健在)
- `death_date`: 死亡时间(注销时填写)
- `del_flag`: 删除标志 ('0'-正常, '2'-已删除)

**有效人员查询条件**:
```sql
WHERE del_flag = '0' AND is_alive = '1'
```

---

## 🎓 最佳实践

1. **业务办理场景**: 优先使用`selectAliveSubsidyPersonByIdCardNo`或`selectAliveSubsidyPersonById`
2. **综合查询场景**: 使用`selectSubsidyPersonByIdCardNo`或`selectById`,并在UI上明确标识注销状态
3. **列表查询**: 通过`isAlive`参数动态控制,默认可不传(查所有),或传'1'(只查健在)
4. **前端调用**: 根据场景传递`includeCancel`参数,业务办理传false(默认),综合查询传true

---

## 📅 修改日期

2025-01-25

## 👤 修改人

系统重构

---

## 🔗 相关文档

- [人员注销登记功能说明](./失地居民身份字段修改内容.md)
- [补贴身份修改影响分析](./补贴身份修改影响分析.md)
