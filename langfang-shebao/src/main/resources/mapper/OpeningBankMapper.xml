<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.shebao.mapper.OpeningBankMapper">

    <select id="countBusinessReferences" resultType="int">
        select
            (select count(1) from benefit_determination
             where del_flag = '0' and grant_org = #{code})
          + (select count(1) from shebao_payment_plan_detail
             where del_flag = '0' and grant_org = #{code})
          + (select count(1) from shebao_payment_plan_summary
             where del_flag = '0' and grant_org = #{code})
    </select>
</mapper>
